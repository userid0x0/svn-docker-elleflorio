#!/command/execlineb -S0

if { /command/s6-test $# -eq 2 }

backtick -in FILENAME {
    pipeline { /command/s6-echo "${1}" }
    tr "a-z" "A-Z"
}
import -u FILENAME

redirfd -w 1 /var/run/s6/container_environment/${FILENAME}
/command/s6-echo -n -- ${2}
